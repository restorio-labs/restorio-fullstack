{
  "name": "@restorio/api-client",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist && rm -rf node_modules",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" && eslint . --fix",
    "check": "prettier --check \"**/*.{ts,tsx,json,md}\" && eslint . --ext ts,tsx"
  },
  "dependencies": {
    "@restorio/types": "*",
    "@restorio/auth": "*",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "typescript": "^5.9",
    "vite": "^5.4.0",
    "vitest": "^4.0.16",
    "@vitest/coverage-v8": "^4.0.16"
  }
}
